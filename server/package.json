{
  "name": "@simon/server",
  "type": "module",
  "types": "src/types.ts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "pnpm wrangler deploy --minify src/index.ts",
    "dev": "pnpm wrangler dev --live-reload src/index.ts",
    "lint": "eslint .",
    "test": "vitest run",
    "start": "wrangler dev",
    "typecheck": "pnpm exec tsc --build"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.4.16",
    "vitest": "1.5.0"
  }
}
